<head>
    <title><%= params['name'] %></title>
</head>
<body>
<h1><%= params['name'] %></h1>
<% @recipe_info.each do |recipe| %>
  <ul>
    <a href="/recipes/<%= "#{recipe['id']}" %>"><%= "#{recipe['name']}" %></a>
  </br>
    <%= "#{recipe['description']}" %>
  </ul>
<% end %>
</body>
